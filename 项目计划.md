# 基于监督微调的医学推理对话大模型

## 项目背景与目标
- 业务背景：通用大模型在医学垂域存在专业性不足、缺乏显式推理链的问题；场景为医学问答/辅助咨询。
- 核心问题：提升回答的专业性与逻辑性，让模型输出“先思考(think)后作答(answer)”的结构化响应。
- 预期成果：得到一个能稳定输出 `<think>...</think>` + 结论 的微调大模型；价值在于可解释性、领域对齐、轻量可部署。

## 数据集分析与处理
- 数据来源：开源数据 delicate_medical_r1_data（含 R1 风格思维链的中文医学问答），该数据由基于 metagpt 搭建的造数据的 workflow 生成，使用 qwq 模型进行每 query 10 次 think 过程的生成。
- 数据规模：2000+ 条，按 9:1 划分为训练集和验证集。
- 原始字段：
    1. question：用户提出的问题，即模型的输入
    2. think：模型的思考过程
    3. answer：模型思考完成后，回复的内容
    4. metrics：只有 f1-score。查阅该开源数据可知，生成数据时，经过若干步骤后得到了带有 N 步推理的思维链。使用 llm 找到其中的关键步骤，然后依次判断这些关键步骤解决的问题或陈述的事实是否出现在真实答案中，得到召回率；以真实答案为基准对每个步骤进行正确性的评估，得到精确率；从而计算得到 F1 分数。
- 预处理流程：将原始行转为 `{instruction, input, output}` 三元组形式，其中
    1. instruction = PROMPT，初始设计 PROMPT = "你是一个医学专家，你需要根据用户的问题，给出带有思考的回答。"
    2. input = question
    3. output = f"<think>{think}</think> \n {answer}"
- 质量控制：
    1. 长度分析：先统计 token 长度分布，确认 MAX_LENGTH=2048 下被截断的样本比例（截断会切掉思考链/答案尾部，产生“学不会停止”的坏样本），比例过高则上调上限或对超长思维链做压缩。
    2. 序列终止符：拼接时结尾应使用 `eos_token`（而非直接用 pad_token 充当结尾），保证模型学会自然停止生成。
    3. 脏数据过滤：剔除 think/answer 为空、缺字段或明显异常的样本。

### 数据处理流程详解
1. **数据下载与切分（`data/download.py`）**
   - 通过 `MsDataset.load('krisfu/delicate_medical_r1_data', split='train')` 从 ModelScope 拉取原始数据。
   - 使用 `random.seed(42)` 固定随机种子后 `random.shuffle`，保证切分可复现；按 `split_idx = int(len(data_list) * 0.9)` 做 9:1 切分，分别写入 `train.jsonl` 与 `val.jsonl`（`ensure_ascii=False` 保留中文）。
   - 输出规模日志：训练集约 1800+、验证集约 200+ 条。
2. **格式转换（`dataset_jsonl_transfer`）**
   - 读取原始 JSONL，逐行 `json.loads`，抽取 `question / think / answer`。
   - 拼接监督目标：`output = f"<think>{think}</think> \n {answer}"`，将“思考链 + 结论”作为一个整体作为模型需要学习生成的内容。
   - 输出为 `train_format.jsonl` / `val_format.jsonl`，字段为 `{instruction, input, output}`。
   - 幂等设计：仅当格式化文件不存在时才重新生成（`if not os.path.exists(...)`），避免重复处理。
3. **Tokenize 与 Label Mask（`process_func`）**
   - 采用 Qwen ChatML 模板手动拼接：
     `<|im_start|>system\n{PROMPT}<|im_end|>\n<|im_start|>user\n{input}<|im_end|>\n<|im_start|>assistant\n`。
   - `input_ids = instruction_ids + response_ids + [pad_token_id]`；`attention_mask` 同步拼接末尾补 `1`。
   - **关键点：Loss Mask**。`labels = [-100]*len(instruction_ids) + response_ids + [pad_token_id]`，即把 system+user 部分标为 `-100`（不计损失），模型只在 assistant 的思考链与答案上计算 loss。这保证模型“学会回答”，而不是“背诵问题”。
   - 超过 `MAX_LENGTH=2048` 的样本对 `input_ids / attention_mask / labels` 三者同步截断，防止 OOM 与显存溢出。
4. **构建 Dataset**：`pd.read_json(..., lines=True)` → `Dataset.from_pandas` → `.map(process_func, remove_columns=...)`，训练集/验证集分别得到只含 `input_ids / attention_mask / labels` 的张量数据。
5. **动态 Padding**：训练时由 `DataCollatorForSeq2Seq(tokenizer, padding=True)` 在 batch 内动态补齐，减少无效计算。

## 模型选型与架构
### 对照实验设计（控制变量，避免“模型规模 × 微调方法”混杂）
| 实验 | 模型 | 微调方法 | 作用 |
| --- | --- | --- | --- |
| Baseline | Qwen3-1.7B | 不微调（zero-shot） | 衡量 SFT 带来的净增益 |
| Exp A | Qwen3-1.7B | 全量微调 | 主方案 |
| Exp B | Qwen3-1.7B | LoRA | 与 A 同规模，隔离“微调方法”变量 |
| Exp C | Qwen3-8B | LoRA | 与 B 同方法，隔离“模型规模”变量 |
- 说明：原计划把“1.7B 全量”与“8B LoRA”直接对比会同时改变规模和方法两个变量，无法归因差异来源；上表通过 Baseline + 控制变量的 B/C 使结论可解释。
- 资源受限时优先跑 Baseline + Exp A + Exp B；Exp C 视显存情况选做（8B 全量微调显存开销大，故用 LoRA）。

### 当前实现的模型与微调策略（对应 `train.py`）
- **基座模型**：`Qwen/Qwen3-1.7B`，通过 `snapshot_download` 从 ModelScope 下载到本地缓存目录。
- **加载配置**：
  - `AutoTokenizer.from_pretrained(..., use_fast=False, trust_remote_code=True)`。
  - `AutoModelForCausalLM.from_pretrained(..., device_map="auto", torch_dtype=torch.bfloat16)`：使用 bf16 半精度加载，降低显存占用并保持数值稳定性。
- **微调策略**：**全参数微调（Full Fine-tuning）**，非 LoRA。全量更新所有权重，配合以下显存优化：
  - `model.enable_input_require_grads()`：开启梯度检查点时必须调用，使输入张量参与梯度计算。
  - `gradient_checkpointing=True`：以时间换显存，反向传播时重算激活值。
- **训练目标**：标准的因果语言建模（Causal LM）Next-Token Prediction，仅在 assistant 段计算交叉熵损失。

### 关键训练参数设置（`TrainingArguments`）
| 参数 | 取值 | 说明 |
| --- | --- | --- |
| `per_device_train_batch_size` | 1 | 单卡单样本，受显存约束 |
| `gradient_accumulation_steps` | 4 | 梯度累积，等效 batch size = 1×4 = 4 |
| `per_device_eval_batch_size` | 1 | 验证同样单样本 |
| `num_train_epochs` | 2 | 训练 2 个 epoch |
| `learning_rate` | 1e-4（原始） | **偏大**：全量微调建议 1e-5 ~ 2e-5，1e-4 更适合 LoRA（见下方调整建议） |
| `eval_strategy` / `eval_steps` | steps / 100 | 每 100 步在验证集评估一次 |
| `logging_steps` | 10 | 每 10 步记录一次训练指标 |
| `save_steps` | 400 | 每 400 步保存一次 checkpoint |
| `gradient_checkpointing` | True | 显存优化 |
| `report_to` | swanlab | 训练监控接入 SwanLab |
| `output_dir` | /root/autodl-tmp/output/Qwen3-1.7B | 权重输出目录 |

### 参数合理性评估与调整建议
- **学习率**：`1e-4` 对 1.7B 全量微调偏大，易训练震荡/灾难性遗忘。调整为：全量微调 `1e-5`（范围 7e-6~2e-5）、LoRA `1e-4`（范围 1e-4~2e-4）。
- **学习率调度**：原配置无 warmup 与衰减。增加 `warmup_ratio=0.05` + `lr_scheduler_type="cosine"`，稳定收敛。
- **正则**：增加 `weight_decay=0.01`、`max_grad_norm=1.0`（梯度裁剪）抑制过拟合与梯度爆炸。
- **最优 checkpoint 选择**：增加 `load_best_model_at_end=True` + `metric_for_best_model="eval_loss"` + `save_total_limit=3`，并让 `save_steps` 为 `eval_steps` 整数倍（统一为 100），自动保留验证集最优权重、避免占满磁盘。
- **精度**：训练侧显式设 `bf16=True`（与模型加载一致）。
- **可复现**：设置 `seed=42`。
- **epoch**：数据仅 2000+，`2~3` epoch 合理，配合 eval_loss 判断早停，避免过多轮次过拟合。

## Prompt 工程设计
- 初始 System Prompt 角色设定 + 输出格式约束："你是一个医学专家，你需要根据用户的问题，给出带有思考的回答。"
- 行为影响：训练与推理 Prompt 必须一致（`train.py` 与 `predict.py` 中的 system content 完全相同，否则会造成训练-推理分布不匹配）。
- 输出格式约束：通过数据侧强约束 `<think>...</think> \n {answer}` 结构，让模型隐式学会“先思考后作答”。
- 可做实验：
    1. Prompt Engineering：提示词设计，可以对比不同 Prompt 对实验的影响（如是否显式要求 `<think>` 标签、是否加入安全免责声明等）。

## 评估指标体系
1. **训练过程监控（SwanLab，自动化）**
   - 项目名 `SWANLAB_PROJECT="qwen3-sft-medical"`，`run_name` 按对照实验命名（如 `qwen3-1.7B-full` / `qwen3-1.7B-lora`）；通过 `swanlab.config.update` 记录 model / prompt / lr / 微调方法等超参。
   - 实时曲线：`train/loss`（每 10 步）、`eval/loss`（每 100 步）、`learning_rate`、`grad_norm`、`epoch` 等。
   - 判读要点：train_loss 应平滑下降；eval_loss 若先降后升即过拟合，据此由 `load_best_model_at_end` 选取最优 checkpoint。
2. **验证集定量评估（`evaluate.py`，离线批量）**
   - `eval_loss` / 困惑度 PPL（`exp(eval_loss)`）：训练侧自动产出。
   - **格式合规率**：正则校验输出是否成对且顺序正确地包含 `<think>...</think>` 且答案非空，衡量结构化能力。
   - **语义相似度**：用 embedding 模型（默认 `Qwen/Qwen3-Embedding-0.6B`，兼顾精度与显存；显存充裕可换 `Qwen3-Embedding-4B`）计算“生成答案（去除 think 段）”与参考 answer 的余弦相似度均值，衡量内容贴合度。
   - 所有实验（Baseline / A / B / C）在同一验证集上跑同一套指标，横向对比。
3. **医学准确性评估（进阶，可选）**
   - 复用数据集 F1 思路：用 LLM 抽取回答关键推理步骤与参考 answer 逐步比对，得到精确率/召回率/F1。